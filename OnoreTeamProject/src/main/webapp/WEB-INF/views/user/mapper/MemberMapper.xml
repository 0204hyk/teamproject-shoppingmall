<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.onore.project.MembersXmlMapper">


	<!-- 회원가입 -->
	<insert id="memJoin">
		INSERT INTO members
			(mem_id, mem_pw, mem_email mem_phone, mem_name, 
			mem_gender, mem_birth_date, mem_register_date, mem_point, mem_sms) 
		VALUES 
			(#{mem_id}, #{mem_pw}, #{mem_email}, #{mem_phone}, 
			#{mem_name}, #{mem_gender},	#{mem_birth_date}, #{mem_register_date}, 3000, #{mem_sms})
	</insert>
	
	
		<select id="getUserInfo" resultType="com.onore.project.member.dto.MemberDTO">
		SELECT
			mem_id, mem_pw, mem_email mem_phone, mem_name, 
			mem_gender, mem_birth_date, mem_register_date, mem_point, mem_sms
		FROM
			members
		WHERE
			mem_id = #{mem_id}
	</select>
	
	<select id="getUserList" resultType="com.onore.project.member.dto.MemberDTO">
		SELECT
			mem_id, mem_pw, mem_email mem_phone, mem_name, 
			mem_gender, mem_birth_date, mem_register_date, mem_point, mem_sms
		FROM
			members
	</select>
	
	<insert id="insertUser" parameterType="com.freehoon.web.user.model.MemberDTO">
		INSERT INTO members (mem_id, mem_pw, mem_email mem_phone, mem_name, 
			mem_gender, mem_birth_date, mem_register_date, mem_point, mem_sms)
		VALUES (#{mem_id}, #{mem_pw}, #{mem_email}, #{mem_phone}, 
			#{mem_name}, #{mem_gender},	#{mem_birth_date}, now(), 3000, #{mem_sms})
	</insert>
	
	<update id="updateUser" parameterType="com.onore.project.member.dto.MemberDTO">
		UPDATE members SET
			mem_name = #{mem_name}
			, mem_pw = #{mem_pw}
			, mem_email = #{mem_email}
			, mem_phone = #{phone}
		WHERE
			mem_id = #{mem_id}
	</update>
	
	<delete id="deleteUser" parameterType="int">
		DELETE FROM members
		WHERE mem_id = #{mem_id}
	</delete>
	
</mapper>