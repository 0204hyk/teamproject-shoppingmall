<?xml version="1.0" encoding="UTF-8"?>				
<!DOCTYPE mapper				
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"			
	"https://mybatis.org/dtd/mybatis-3-mapper.dtd">			
<mapper namespace="com.onore.project.member.mapper.MemberMapper">				

<insert id="memJoin" parameterType="com.onore.project.member.dto.Member">
	INSERT INTO members VALUES (
		#{mem_id},
		#{mem_pw},
		#{mem_email},
		#{mem_phone},
		#{mem_name},
		#{mem_zip_code},
		#{mem_street_address},
		#{mem_detail_address},
		#{mem_gender},
		#{mem_birth_date},
		SYSDATE,
		3000,
		#{mem_sms}	
	)
</insert>

<select id="memberLogin" resultType="com.onore.project.member.dto.Member">
	SELECT mem_name FROM members WHERE mem_id = #{mem_id} AND mem_pw = #{mem_pw}
</select>
</mapper>				
