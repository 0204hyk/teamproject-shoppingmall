<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.onore.project.mapper.Shop1Mapper">
	<select id="getDetail" resultType="com.onore.project.dto.ProductDTO">
		SELECT * FROM products WHERE product_num=3
	</select>
	
	<select id="getProReview" resultType="com.onore.project.dto.ReviewDTO">
		SELECT * FROM reviews WHERE product_num=3
	</select>
</mapper>